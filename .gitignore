node_modules/
out/
cache/
broadcast/
.env
# The keeper agent: its secrets are never plaintext on disk except during a local dry
# run, and its world is regenerated by agent/stage.sh.
agent/keeper.env
agent/keeper.env.ring
agent/world.json
# Machine-local Claude Code state: permissions and a launch.json with an absolute path.
# .claude/skills/ (Ledger's wallet-cli and DMK skills, from LedgerHQ/agent-skills) IS committed:
# the Ledger flow in this repo follows their rules
.claude/settings.local.json
.claude/launch.json
*.log
.DS_Store
# What ui/walk.sh writes on the live anvil walk (identical to results/price-path.* to the
# precision the artifact prints; see the script's header), and the page's build.
results/live-path.txt
results/live-path.json
ui/node_modules/
ui/dist/
# Camera originals carry GPS EXIF. Only the metadata-free collage the feedback document embeds
# is committed.
photos/*
!photos/nano-x-blind-signed-call.png
